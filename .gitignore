# Python
__pycache__/
*.pyc
.venv/
.venv-build/
venv/

# PyInstaller build output (the .spec IS tracked; build/ and dist/ are not)
build/proctor-client/
dist/

# Downloaded ffmpeg binary (fetched by build/fetch_ffmpeg.* at build time)
vendor/

# Local machine config (ship proctor.ini.example, not the filled-in one)
proctor.ini

# Runtime data (generated)
storage/
client_queue/
*.mp4
*.log
